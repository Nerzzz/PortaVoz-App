package com.example.portavoz;

import com.google.firebase.auth.FirebaseAuth;
import com.google.firebase.auth.FirebaseUser;

public class User {
    String id_, username, email, fName, lName, image, about, banner, reportsResetAt, createdA, upDatedAt;
    int followers, following, unreadNofications, remaningReports, totalReports, __v;

    public User(String id_, String username, String image){

    }
}
