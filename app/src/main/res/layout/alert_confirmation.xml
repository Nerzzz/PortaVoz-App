<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/lay_rounded_color">

    <TextView
        android:id="@+id/confirmation_txtTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="20dp"
        android:fontFamily="@font/poppins_semibold"
        android:includeFontPadding="false"
        android:text="Title"
        android:textColor="?attr/txtColor"
        android:textSize="18sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/confirmation_txtDesc"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:fontFamily="@font/poppins"
        android:text="Subtitle"
        android:textColor="?attr/txtColor"
        android:textSize="15sp"
        app:layout_constraintEnd_toEndOf="@+id/confirmation_txtTitle"
        app:layout_constraintStart_toStartOf="@+id/confirmation_txtTitle"
        app:layout_constraintTop_toBottomOf="@+id/confirmation_txtTitle" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/confirmation_btnConfirm"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp"
        android:backgroundTint="@color/gradienteCenter"
        android:fontFamily="@font/poppins_medium"
        android:includeFontPadding="false"
        android:text="Button"
        android:textColor="@color/white"
        android:textSize="15sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/confirmation_txtDesc"
        app:layout_constraintTop_toBottomOf="@+id/confirmation_txtDesc" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/confirmation_btnCancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="10dp"
        android:backgroundTint="@android:color/transparent"
        android:fontFamily="@font/poppins_medium"
        android:includeFontPadding="false"
        android:text="Button"
        android:textColor="?attr/txtColor"
        android:textSize="15sp"
        app:layout_constraintBottom_toBottomOf="@+id/confirmation_btnConfirm"
        app:layout_constraintEnd_toStartOf="@+id/confirmation_btnConfirm"
        app:layout_constraintTop_toTopOf="@+id/confirmation_btnConfirm"
        app:strokeColor="?attr/border1"
        app:strokeWidth="1dp" />
</androidx.constraintlayout.widget.ConstraintLayout>